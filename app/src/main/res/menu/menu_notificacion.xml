<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@+id/action_eliminar"
        android:title="ELiminar"
        android:visible="false"
        android:icon="@mipmap/ic_action_eliminar"
        app:showAsAction="always"/>

    <item android:id="@+id/action_update"
        android:title="Actualizar"
        android:icon="@drawable/ic_action_update"
        app:showAsAction="always"/>

    <item android:id="@+id/action_habilitar"
        android:icon="@drawable/ic_action_habilitar"
    android:title="Habilitar permiso"
    app:showAsAction="always"/>
</menu>